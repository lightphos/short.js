{
  "type": "module",
  "scripts": {
    "build:st": "node compile/cmp.mjs -o /tmp/short/",
    "watch:st": "node compile/cmp.mjs -o /tmp/short/ --watch",
    "tw": "npx @tailwindcss/cli -i ./app/app.tcss -o /tmp/short/app/app.css --watch",
    "server": "npx http-server /tmp/short/app -p 3000 --cors -c-1",
    "watch": "concurrently \"npm run tw\" \"npm run watch:st\" \"npm run server\" \"npm run watch:assets\"",
    "copy:assets": "node compile/copy-assets.mjs",
    "watch:assets": "npm run copy:assets && onchange \"app/**/*.js\" \"app/**/*.css\" \"app/**/*.html\" \"short.js\" -- node compile/copy-assets.mjs \"{{file}}\"",
    "test": "vitest run",
    "watch:bun": "concurrently \"bun run tw\" \"bun run watch:st\" \"bun run server\" \"bun run watch:assets\"",
    "test:bun": "bun run test"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "jsdom": "^30.0.1",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "onchange": "^7.1.0",
    "vitest": "^5.0.1",
    "bun": "^1.4.2"
  }
}
